# TaskFlow 默认 BYOK（自带 Key），完全可以不创建 .env 直接运行。
# DeepSeek API Key 在应用内「AI 设置」里填写，存在浏览器本地，不读取此文件。
#
# 下面这些都是可选项，只有想改默认行为时才需要。复制为 .env 后按需取消注释：

# 兼容 DeepSeek 的自定义网关地址（默认官方）
# DEEPSEEK_BASE_URL=https://api.deepseek.com

# 默认模型名（应用内设置里选的模型会覆盖它）
# AI_PLANNER_MODEL=deepseek-chat

# 【headless 回退，显式 opt-in】给无浏览器的调用方（MCP plan_tasks 等）用的
# AI Key：POST /api/plan 请求体不带 apiKey 时才回退到它。网页端仍是纯 BYOK
# （Key 只存浏览器 localStorage，不读取此变量）。千万不要把真实 Key 提交进仓库。
# TASKFLOW_AI_KEY=sk-...
