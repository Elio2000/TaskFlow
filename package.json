{
  "name": "taskflow",
  "version": "1.0.0",
  "description": "本地优先、单人使用的 AI 任务规划工具（BYOK DeepSeek）。",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && (lsof -ti:3001 | xargs kill -9 2>/dev/null || true) && npm run dev",
    "dev:client": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "start": "cd server && npm run start",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "test": "node --test tests/*.test.mjs"
  },
  "keywords": ["task-manager", "todo", "planner", "ai", "deepseek", "local-first", "byok", "self-hosted", "react", "typescript"],
  "author": "",
  "repository": { "type": "git", "url": "https://github.com/Elio2000/TaskFlow.git" },
  "homepage": "https://github.com/Elio2000/TaskFlow#readme",
  "bugs": "https://github.com/Elio2000/TaskFlow/issues",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
